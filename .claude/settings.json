{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(*git commit*)",
            "command": "{ { cargo fmt --all -- --check || echo '[warn] cargo fmt found formatting differences (non-blocking; run: cargo fmt --all)'; } && scripts/check-pii.sh && scripts/check-floats.sh && cargo test --workspace --quiet 2>&1 && cargo clippy --all-targets --quiet -- -D warnings 2>&1; } || exit 2"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(cargo test *)",
      "Bash(cargo build *)",
      "Bash(cargo clippy *)",
      "Bash(cargo fmt --all -- --check)",
      "Bash(scripts/extract_pr_body.sh *)",
      "Bash(scripts/next_pr_number.sh)",
      "Bash(scripts/review_path.sh *)"
    ]
  }
}
