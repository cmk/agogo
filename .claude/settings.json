{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cargo fmt -p agogo-chan -p agogo-core -p agogo-cli -- --check && scripts/check_pii.sh && scripts/check_floats.sh && scripts/check_layers.sh && scripts/check_connections.sh && scripts/check_boundary_panics.sh"
          }
        ]
      }
    ]
  }
}
