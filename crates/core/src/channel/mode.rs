//! `ChannelMode` — the enum of v0.1 output modes. Stub: only the
//! `MidiClock` variant is rendered in Plan 03; the remaining variants
//! exist so downstream pattern matches are exhaustive against the full
//! spec surface. Their per-channel rendering lands in v0.2+.
